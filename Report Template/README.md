# Credit Report Template

Small React template for a credit report UI.

Quick start

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

Notes
- Uses Vite + React.
- Styling uses only shades of green and gray and the Verdana font.
